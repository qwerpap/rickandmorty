import 'package:flutter/material.dart';

class AppColors {
  static const whiteColor = Colors.white;
  static const blackColor = Colors.black;
  static const greyColor = Colors.grey;
  static const primaryColor = Color.fromRGBO(15, 176, 200, 1);
  static const navActiveColor = primaryColor;
  static const navInactiveColor = Colors.grey;
}
